<h1> Add a Task</h1>
<%= form_for [@list, @task] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true %>
  <br />
  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit %>

<% end %>